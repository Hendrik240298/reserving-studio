# Legacy GUI API Chat Archive

This archive contains the old dashboard, REST API, AI chat loop, session state, and related tests/docs.

These files are not active Harness Native Reserving Studio architecture.

Rules:

- Do not add new active-runtime dependencies from `source/` or `harness/` into this archive.
- Do not route new harness tools through this code.
- If a concept is still useful, promote or reimplement it in active `source/` or `harness/` code.
- Treat the archive as historical reference, not as the active design center.
